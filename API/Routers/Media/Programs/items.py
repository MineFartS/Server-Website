from philh_myftp_biz.web import URL

class Blender:

    class Windows:
        url  = URL('https://mirrors.iu13.net/blender/release/Blender3.6/blender-3.6.23-windows-x64.msi')
        ext = 'msi'

class Disk_Drill:

    class Windows:
        url = URL("https://win.cleverfiles.com/disk-drill-win.exe")
        ext = "exe"

class disk2vhd:
    
    class Windows:
        url = URL("https://live.sysinternals.com/disk2vhd64.exe")
        ext = "exe"

class Github_Desktop:

    class Windows:
        url = URL("https://central.github.com/deployments/desktop/desktop/latest/win32")
        ext = "exe"

class htTrack:

    class Windows:
        url = URL("https://download.httrack.com/cserv.php3?File=httrack_x64.exe")
        ext = "exe"

class IIS_Rewrite:

    class Windows:
        url = URL("https://download.microsoft.com/download/1/2/8/128E2E22-C1B9-44A4-BE2A-5859ED1D4592/rewrite_amd64_en-US.msi")
        ext = "msi"

class LTspice:

    class Windows:
        url = URL("https://ltspice.analog.com/software/LTspice64.msi")
        ext = "msi"

class Make:

    class Windows:
        url = URL("https://gigenet.dl.sourceforge.net/project/gnuwin32/make/3.81/make-3.81.exe?viasf=1")
        ext = "exe"


class Remove_MS_Edge:

    class Windows:
        url = URL("https://github.com/ShadowWhisperer/Remove-MS-Edge/releases/latest/download/Remove-Edge.exe")
        ext = "exe"

class Rockstar_Games_Launcher:

    class Windows:
        url = URL("https://gamedownloads.rockstargames.com/public/installer/Rockstar-Games-Launcher.exe")
        ext = 'exe'

class Steam:

    class Windows:
        url = URL("https://cdn.fastly.steamstatic.com/client/installer/SteamSetup.exe")
        ext = 'exe'

class YouTube_DL:
    
    class Windows:
        url = URL("https://www.github.com/ytdl-org/ytdl-nightly/releases/latest/download/youtube-dl.exe")
        ext = 'exe'

class Wake_On_Lan:

    class Windows:
        url = URL("https://github.com/basildane/WakeOnLAN/releases/download/2.12.4/WakeOnLAN_2.12.4.0.exe")
        ext = 'exe'

class VS_Code:

    class Windows:
        url = URL("https://code.visualstudio.com/sha/download?build=stable&os=win32-x64")
        ext = 'exe'
        
class TeraCopy:

    class Windows:
        url = URL("https://www.codesector.com/files/teracopy.exe")
        ext = 'exe'

class SurfShark:

    class Windows:
        url = URL("https://downloads.surfshark.com/windows/latest/SurfsharkSetup.exe")
        ext = 'exe'

class Ollama:

    class Windows:
        url = URL("https://ollama.com/download/OllamaSetup.exe")
        ext = 'exe'

class Minecraft_Launcher:

    class Windows:
        url = URL("https://launcher.mojang.com/download/MinecraftInstaller.exe")
        ext = "exe"

class Discord:

    class Windows:
        url = URL("https://discord.com/api/downloads/distributions/app/installers/latest?channel=stable&platform=win&arch=x64")
        ext = "exe"